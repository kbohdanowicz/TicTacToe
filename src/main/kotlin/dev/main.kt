package dev

fun main() {
    val game = TicTacToe(5,4, 2,3)
    game.newGame()
    //val list = List(5) { it }
    //println(list.flatMap { it })

    // val matrix = Matrix(5, 3) { it }
    // val matrix2 = Matrix(3 by 5) { it }
    //
    // println(matrix.show())
    // println(matrix.transposed().show())

    //println(matrix.dimensions)
    //println(matrix.itemCount)
}